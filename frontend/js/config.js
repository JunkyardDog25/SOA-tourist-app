// Relativni URL: nginx na :3000 prosleđuje /api → api-gateway (vidi frontend/nginx.conf)
export const API_BASE_URL = window.TOUR_APP_API_URL || "/api";

export const DEFAULT_MAP_CENTER = [44.8176, 20.4633];
export const DEFAULT_MAP_ZOOM = 13;
